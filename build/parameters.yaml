VPN_PACK_EMAIL: "example@example.com"
VPN_PACK_DOMAIN_NAME: "localhost"

WG_PORT: 51820
WG_SUB_NETWORK: "10.0.0.0/24"
WG_PIHOLE_IP: "10.0.0.53"

WG_UP_SCRIPT: "iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE"
WG_DOWN_SCRIPT: "iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE"

INTERNAL_SUBNET: "192.168.1.0/24"
INTERNAL_GATEWAY: "192.168.1.1"

INTERNAL_WG_IP: "192.168.1.2"
INTERNAL_PIHOLE_IP: "192.168.1.3"

PIHOLE_PASSWORD: "admin"

# append keys (we should have empty line after this line for correct generation)
